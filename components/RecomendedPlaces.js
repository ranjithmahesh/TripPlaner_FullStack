import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const RecomendedPlaces = () => {
  return (
    <View>
      <Text>RecomendedPlaces</Text>
    </View>
  )
}

export default RecomendedPlaces

const styles = StyleSheet.create({})